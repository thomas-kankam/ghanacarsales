# Include this in the server { } block for backend.omnicarsgh.com
# (or paste the directive directly), then:
#   sudo nginx -t && sudo systemctl reload nginx
#
# Car uploads send images as base64 in JSON (~1.33× binary size).
# Multiple JPGs easily exceed nginx's default 1m limit → HTTP 413.

client_max_body_size 64m;
