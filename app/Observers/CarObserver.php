<?php

namespace App\Observers;

// use App\Models\Car;

class CarObserver
{
    // public function created(Car $car): void
    // {
    //     if ($car->status === 'published' && class_exists(\App\Services\AlertService::class)) {
    //         app(\App\Services\AlertService::class)->checkAlertsForCar($car);
    //     }
    // }

    // public function updated(Car $car): void
    // {
    //     if ($car->wasChanged('status') && $car->status === 'published' && class_exists(\App\Services\AlertService::class)) {
    //         app(\App\Services\AlertService::class)->checkAlertsForCar($car);
    //     }
    // }
}
